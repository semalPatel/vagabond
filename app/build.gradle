apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'
apply plugin: 'kotlin-android-extensions'
apply plugin: 'kotlin-kapt'
apply from: '../dependencies.gradle'


buildscript {
    repositories.mavenLocal()
}

androidExtensions {
    experimental = true
}

android {
    compileSdkVersion 28
    defaultConfig {
        applicationId "com.sierra.vagabond"
        minSdkVersion 21
        targetSdkVersion 28
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation kotlinDependencies.values()
    implementation uiDependencies.values()
    implementation roomDependencies.values()
    implementation networkingDependencies.values()
    implementation firbaseDependencies.values()
    implementation rxJava2Dependencies.values()
    kapt roomCompiler.values()
    androidTestImplementation testingDependencies.values()
}

repositories {
    mavenCentral()
}

apply plugin: 'com.google.gms.google-services'
